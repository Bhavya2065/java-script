const id = 20003737 
// Value is constant

let name = 'Bhavya' 
// value can be updated

var age = 13 
// it doesn`t use nowdays

dob = '13/12/2002' 
// this is also a type of assigning value but cannot use anywhere.

let account; 
// only assigning the value

// id = 6534765 (cannot assign other value because of const variable)
name = 'Sanjiv' // It is generally not used.

console.log(id); 
// to print id 


/* The unique idea to print all information in one table form */
console.table([id,name,age,dob,account]);


/* Note: Only numbers are preferable without singlecod (''), not string or any character. */
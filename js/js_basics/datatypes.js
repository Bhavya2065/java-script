"use strict"; // treat all code as newer version

let name = "Bhavya" // String 
let age = 19 // number
let isLoggedin = false // boolean
let a = null // contain empty field / value doesn`t assign (Standalone value)
let b = undefined // cannot contain any value

/* Other datatypes:
        1.symbol
        2.bigint
        3.object (most important)
*/

console.table([name,age,isLoggedin,a,b])

// "typeof" is used for knowing the datatype of any value.
console.log(typeof "Bhavya");
console.log(typeof 19);
console.log(typeof false);
console.log(typeof null); // by default the typeof null is object. */ interview Ouestion /*
console.log(typeof undefined); // by default the typeof undefined is undefined. */ interview Ouestion /*
